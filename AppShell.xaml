<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FlyoutBackgroundColor.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FlyoutBackgroundColor"
    FlyoutBehavior="Flyout">

    <!--  Both work here:
    <Shell.FlyoutBackgroundColor>
        <x:StaticResource>Tertiary</x:StaticResource>
    </Shell.FlyoutBackgroundColor>
    <Shell.FlyoutBackground>
        <x:StaticResource>TertiaryBrush</x:StaticResource>
    </Shell.FlyoutBackground>
    -->

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

</Shell>
