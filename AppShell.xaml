<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FlyoutBackgroundColor.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FlyoutBackgroundColor"
    FlyoutBehavior="Flyout">

    <!-- Both work here:
    <Shell.FlyoutBackgroundColor>{StaticResource Tertiary}</Shell.FlyoutBackgroundColor>
    <Shell.FlyoutBackground>{StaticResource TertiaryBrush}</Shell.FlyoutBackground>
    -->

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

</Shell>
